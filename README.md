# ManMath

ManMath is a V2-only practice platform for Vietnamese high-school mathematics.
Published content is immutable, attempts are pinned to an `ExamVersion`, and scoring is performed on the backend.

See [architecture](docs/ARCHITECTURE.md), [API](docs/API.md), [content import](docs/IMPORT_JSON.md), and [testing](docs/TESTING.md).
