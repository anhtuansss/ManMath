# Disposable PostgreSQL database only. It must end in `_verify`.
# Do not put real credentials in this committed file.
VERIFY_DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/manmath_verify?schema=public"
VERIFY_DATABASE_CONFIRM=MANMATH_VERIFY_DB
