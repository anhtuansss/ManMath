import { ExamListClient } from '../../../components/exam/ExamListClient';

/**
 * Guest-capable exam workspace. The full workspace layout is added in Phase 0B.
 */
export default function DashboardPage() {
  return <ExamListClient />;
}
