'use client';

export function ExamCatalogToolbar({ value, activeCount, onChange, onOpenFilters }: { value: string; activeCount: number; onChange: (value: string) => void; onOpenFilters: () => void }) {
  return <div className="flex flex-wrap items-center gap-3 rounded-2xl border border-border bg-surface p-2 shadow-[0_2px_10px_rgba(15,23,42,.025)]"><label className="relative min-w-0 flex-1"><span className="sr-only">Tìm kiếm đề thi</span><svg className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><circle cx="11" cy="11" r="6" /><path d="m20 20-4.2-4.2" strokeLinecap="round" /></svg><input value={value} onChange={(event) => onChange(event.target.value)} placeholder="Tìm theo tên đề, trường hoặc nguồn..." className="h-10 w-full rounded-xl bg-background-alt/60 pl-9 pr-3 text-sm text-text-primary outline-none transition placeholder:text-text-muted focus:bg-surface focus:ring-2 focus:ring-primary-light" /></label>{activeCount > 0 ? <span className="rounded-lg bg-primary-light px-3 py-2 text-xs font-semibold text-primary">{activeCount} bộ lọc đang dùng</span> : <span className="hidden px-2 text-xs text-text-muted sm:inline">Khám phá đề phù hợp</span>}<button type="button" onClick={onOpenFilters} className="inline-flex h-10 shrink-0 items-center gap-2 rounded-xl border border-border px-3 text-sm font-semibold text-text-secondary lg:hidden"><span>Lọc</span>{activeCount > 0 ? <span className="rounded-full bg-primary px-1.5 py-0.5 text-xs text-white">{activeCount}</span> : null}</button></div>;
}
