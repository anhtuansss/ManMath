# Content lifecycle

Validate/import a draft, preview it internally, run publish readiness, then publish the immutable version. Create a new draft version for any later content change. The production import example is `backend/src/data/import/thpt-math-2026-001.json`; the verification fixture is `backend/src/test-fixtures/v2-minimal-exam.json`.
