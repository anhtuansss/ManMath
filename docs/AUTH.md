# Authorization

Public exam and practice reads exclude answer keys. A signed-in owner can read their receipt and authorized review. Anonymous attempts can recover only a safe receipt through the time-limited token; they never receive review data.
