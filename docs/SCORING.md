# Scoring

The V2 policy uses ScoreUnits. Grading validates and normalizes submitted V2 responses server-side for single choice, true/false groups, and short answers. Persisted attempts store awarded and maximum ScoreUnits per canonical version question.
